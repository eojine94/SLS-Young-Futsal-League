# SLS FUTSAL LEAGUE PRD v1.2

> 풋살 리그 정보 확인 서비스

## 1. 제품 개요

### 1.1 문제 정의

풋살 리그를 운영하고 있지만, 순위·일정·경기 결과 등을 모두 수기로 정리하고 문서로 관리하고 있다. 리그 참가자나 관심 있는 사람들이 리그 정보를 확인할 수 있는 서비스가 없다.

### 1.2 해결 방법

풋살 리그 정보 확인 서비스를 웹으로 만들어서, 리그에 참여하는 사용자를 포함한 모든 사람들이 리그 순위·일정·결과를 확인할 수 있도록 한다. 관리자는 리그 데이터를 등록·관리할 수 있다.

### 1.3 타겟 사용자

| 구분 | 설명 |
|------|------|
| 일반 사용자 | 풋살 리그에 참여하는 선수 및 리그에 관심 있는 모든 사람 |
| 관리자 | 리그를 운영하며 팀·일정·결과를 등록·관리하는 운영자 |

### 1.4 서비스 형태

- 모바일 앱 스타일 웹 (고정 폭 레이아웃)
- PC: 화면 중앙에 모바일 앱 크기(max-width: screenMaxWidth)로 표시, 양옆 여백
- 모바일: 화면 전체를 차지
- 관리자 페이지도 동일 웹앱 내에서 권한 기반으로 분기

---

## 2. 기술 스택

| 영역 | 기술 | 비고 |
|------|------|------|
| Frontend | React + TypeScript + Vite | SPA |
| Styling | Tailwind CSS | 토스 디자인 참고 |
| 상태관리/데이터 | TanStack Query | 서버 상태 관리 |
| Backend / DB / Auth | Supabase (PostgreSQL + Auth + Storage) | BaaS |
| Hosting | Vercel | CI/CD 자동화 |

---

## 3. 디자인 가이드

- **레퍼런스**: 토스(Toss) 디자인 시스템
- **레이아웃**: 모바일 앱 스타일 고정 폭 (max-width: screenMaxWidth), PC에서는 중앙 정렬 + 그림자
- 깔끔하고 미니멀한 UI, 넉넉한 여백, 명확한 정보 계층
- 색상: 화이트 배경 기반, 포인트 컬러 1개 (예: 블루 계열)
- 폰트: Pretendard 또는 시스템 폰트
- 카드형 UI, 부드러운 라운딩, 그림자 최소화

---

## 4. 사용자 역할 및 권한

### 4.1 일반 사용자

- 홈, 순위, 일정 화면 조회 가능
- 로그인 불필요 (읽기 전용 서비스)

### 4.2 관리자

- 일반 사용자의 모든 기능 + 관리 기능
- 로그인 필요 (Supabase Auth)
- 팀/선수 관리 (생성, 수정, 삭제)
- 일정 등록·수정
- 경기 결과 등록

---

## 5. 네비게이션 구조

하단 고정 네비게이션 바 (Bottom Tab Bar)

### 5.1 일반 사용자

| 탭 | 아이콘 | 화면 |
|----|--------|------|
| 홈 | 🏠 | Home |
| 순위 | 🏆 | Rank |
| 일정 | 📅 | Schedule |

### 5.2 관리자

| 탭 | 아이콘 | 화면 |
|----|--------|------|
| 홈 | 🏠 | Home |
| 순위 | 🏆 | Rank |
| 일정 | 📅 | Schedule (+ 일정 등록·수정) |
| 팀 | 👥 | Team (팀/선수 관리) |
| 결과 | ✏️ | Result (결과 등록) |

### 5.3 관리자 로그인 접근

- 일반 사용자 화면(Home, Rank, Schedule)의 헤더 우측 상단에 열쇠 아이콘(🔑)이 표시된다.
- 열쇠 아이콘을 탭하면 관리자 로그인 화면으로 이동한다.
- 로그인 성공 시 하단 네비게이션에 관리자 전용 탭(팀, 결과)이 추가로 표시된다.

---

## 6. 화면별 상세 기획

---

### 6.0 Login (관리자 로그인)

관리자 기능에 접근하기 위한 로그인 화면이다.

**레이아웃**

- 화면 좌측 상단에 뒤로 가기 버튼(← 아이콘)이 표시된다.
- 화면 중앙에 로고(SLS FUTSAL LEAGUE)와 "관리자 로그인" 텍스트를 표시한다.
- 로고 하단에 로그인 폼을 표시한다.

**입력 항목**

| 항목 | 타입 | 필수 |
|------|------|------|
| 이메일 | 이메일 입력 | O |
| 비밀번호 | 비밀번호 입력 | O |

**기능**

- 뒤로 가기 버튼: 이전 화면으로 돌아간다.
- Supabase Auth를 사용한 이메일/비밀번호 로그인
- 로그인 성공 시 홈 화면으로 이동하며, 하단 네비게이션에 관리자 전용 탭(팀, 결과)이 추가로 표시된다.
- 로그인 실패 시 에러 메시지 표시

---

### 6.0.1 삭제 확인 다이얼로그

팀, 선수 등의 데이터를 삭제할 때 표시되는 공통 확인 다이얼로그이다.

**레이아웃**

- 반투명 오버레이 배경 위에 중앙 정렬된 다이얼로그 카드를 표시한다.
- 경고 아이콘 + "정말 삭제하시겠습니까?" 타이틀 + 안내 문구
- 하단에 "취소" / "삭제" 버튼을 가로로 나란히 배치한다.
- "삭제" 버튼은 빨간색(Destructive) 스타일을 사용한다.

**사용처**

- 선수 삭제 (6.4.6)
- 팀 삭제 (6.4.2)

---

### 6.1 Home

홈 화면은 리그의 핵심 정보를 한눈에 보여주는 대시보드 역할을 한다.

#### 6.1.1 순위 조회 영역

**레이아웃**

- 상단에 "팀별" / "개인별" 2개의 필터 박스가 가로로 나란히 배치된다.
- 필터 박스는 네모 형태의 토글 버튼이며, 선택된 항목은 포인트 컬러로 활성화된다.
- 필터 하단에 순위 리스트가 표시된다.

**데이터 규칙**

- 상위 3개 데이터만 표시한다.
- "상위"의 기준:
  - 팀별: 승리 수가 많은 순서
  - 개인별: 득점 수가 많은 순서
- 동점일 경우 이름(팀명 또는 선수명) 가나다순으로 정렬한다.

**팀별 필터 선택 시 리스트 항목**

- 표시 정보: 팀명, 승리 수

**개인별 필터 선택 시 리스트 항목**

- 표시 정보: 등번호. 이름 (예: 0. 홍길동), 팀명, 득점 수

**기본값**: 팀별 필터 활성화 상태

---

#### 6.1.2 경기 일정 영역

**레이아웃**

- 순위 영역 하단에 배치한다.
- 상단에 "다음 경기입니다." 텍스트를 표시한다.
- 하단에 경기 리스트가 표시된다.

**데이터 규칙**

- 현재 시점 기준으로 가장 가까운 미래 날짜의 경기를 모두 표시한다.
- 해당 날짜에 경기가 여러 개면 모두 표시한다.

**리스트 항목 (한 줄 표시)**

```
FC Thunder vs FC Storm 02.15(토) 14:00 📍 A구장
```

- 대진, 날짜(MM.DD(요일)), 시간(HH:mm), 장소를 한 줄에 표시

---

### 6.2 Rank

순위를 전체적으로 상세 조회할 수 있는 화면이다.

#### 6.2.1 레이아웃 및 플로우

- 화면 상단에 가이드 텍스트를 표시한다. (예: "리그 팀 순위", "팀을 클릭하면 팀 내 선수 순위를 볼 수 있습니다.")
- 화면 진입 시 팀별 순위 테이블이 표시된다.
- 팀을 클릭하면 해당 팀의 개인별 순위(팀 상세)로 이동한다.

#### 6.2.2 팀별 순위 테이블

| 칼럼 | 설명 | 정렬 가능 여부 |
|------|------|----------------|
| 순위 | 현재 정렬 기준 순위 | X |
| 팀명 | 팀 이름 | O (가나다순) |
| 경기 | 총 경기 수 | X |
| 승 | 승리 수 | O |
| 패 | 패배 수 | O |
| 득점 | 팀 총 득점 수 | O |

- **기본 정렬**: 승리 수 내림차순
- 동점 시 팀명 가나다순

#### 6.2.3 팀 상세 (개인별 순위)

팀을 클릭하면 해당 팀의 선수 목록이 개인별 순위로 표시된다.
상단에 가이드 텍스트를 표시한다. (예: "팀 내 선수 순위", "골과 도움 기준으로 정렬할 수 있습니다.")

| 칼럼 | 설명 | 정렬 가능 여부 |
|------|------|----------------|
| 순위 | 현재 정렬 기준 순위 | X |
| 이름 | 등번호. 이름 형식 (예: 0. 홍길동) | O (가나다순) |
| 골 | 개인 득점 수 | O |
| 도움 | 개인 도움 수 | O |

- **기본 정렬**: 골 수 내림차순
- 동점 시 이름 가나다순
- 팀명은 헤더에 이미 표시되므로 각 행에 표시하지 않는다

#### 6.2.4 정렬 인터랙션

- 칼럼 헤더를 탭하면 해당 칼럼 기준으로 정렬한다.
- 같은 칼럼을 다시 탭하면 오름차순 ↔ 내림차순 토글한다.
- 현재 정렬 기준인 칼럼 헤더에 정렬 방향 아이콘(▲ 또는 ▼)을 표시한다.

---

### 6.3 Schedule

경기 일정을 조회하고, 관리자는 일정을 등록·수정할 수 있는 화면이다.

#### 6.3.1 일정 조회 (일반 사용자 + 관리자)

**레이아웃**

- 화면 상단에 가이드 텍스트를 표시한다. (예: "경기 일정", "예정된 경기를 확인할 수 있습니다.")
- 캘린더 없이 리스트 형식으로 표시한다.
- 날짜별로 그룹핑하여 표시한다.
- 가장 가까운 미래 일정부터 표시한다.
- 홈팀/원정팀 구분 없이, 두 팀의 대진만 표시한다.

**일반 사용자 vs 관리자**

- **일반 사용자**: 일정을 조회만 할 수 있다. 경기 항목을 탭해도 아무 동작이 없다.
- **관리자**: 다음의 추가 기능이 제공된다.
  - 가이드 텍스트 아래에 경기 등록 버튼(원형 + 아이콘)이 표시된다.
  - 경기 등록 버튼을 탭하면 일정 등록 화면(6.3.2)으로 이동한다.
  - 예정된 경기를 탭하면 일정 수정 화면(6.3.3)으로 이동한다.

**날짜 헤더**

```
2025.02.15(토)
```

**리스트 항목 (한 줄 표시)**

```
FC Thunder vs FC Storm 14:00 📍 A구장
```

- 대진, 시간(HH:mm), 장소를 한 줄에 표시

**전체 예시**

```
── 2025.02.15(토) ──
FC Thunder vs FC Storm 14:00 📍 A구장
FC Lightning vs FC Wave 16:00 📍 B구장

── 2025.02.22(토) ──
FC Thunder vs FC Lightning 14:00 📍 마포풋살장
```

#### 6.3.2 일정 등록 (관리자 전용)

관리자로 로그인한 경우, 일정 화면에 경기 등록 버튼(원형 + 아이콘)이 표시된다.
버튼을 탭하면 일정 등록 화면으로 이동한다. 헤더에 "일정 등록"이 표시된다.

**입력 항목**

| 항목 | 타입 | 필수 |
|------|------|------|
| 팀 A | 셀렉트 (등록된 팀 목록) | O |
| 팀 B | 셀렉트 (등록된 팀 목록) | O |
| 날짜 | 날짜 선택 | O |
| 시간 | 시간 선택 | O |
| 장소 | 텍스트 입력 | O |

**유효성 검사**

- 팀 A와 팀 B가 동일하면 등록 불가
- 모든 필수 항목 입력 확인

#### 6.3.3 일정 수정 (관리자 전용)

관리자가 예정된 경기를 탭하면 일정 수정 화면으로 이동한다. 헤더에 "일정 수정"이 표시된다.
입력 항목과 유효성 검사는 일정 등록(6.3.2)과 동일하며, 기존 데이터가 미리 채워진 상태로 표시된다.

---

### 6.4 Team (관리자 전용)

팀과 선수를 관리하는 화면이다. 관리자 네비게이션에만 표시된다.

#### 6.4.1 팀 목록

- 등록된 팀을 리스트로 표시한다.
- 각 팀을 탭하면 팀 상세(선수 관리) 화면으로 이동한다.
- "팀 추가" 버튼으로 새 팀 생성 가능

**팀 목록 리스트 항목**

- 팀명
- 선수 수

#### 6.4.2 팀 생성

팀 목록 화면에서 "팀 추가" 버튼을 탭하면 팀 생성 화면으로 이동한다. 헤더에 "팀 생성"이 표시된다.
상단에 가이드 텍스트를 표시한다. (예: "새 팀 등록", "팀명을 입력하여 새로운 팀을 등록하세요.")

**입력 항목**

| 항목 | 타입 | 필수 |
|------|------|------|
| 팀명 | 텍스트 입력 | O |

**유효성 검사**

- 팀명은 필수 입력
- 기존 팀과 동일한 팀명 불가

#### 6.4.3 팀 수정

팀 목록 화면에서 팀을 탭하면 팀 수정 화면으로 이동한다. 헤더에 "팀 수정"이 표시된다.
상단에 가이드 텍스트를 표시한다. (예: "팀 정보 수정", "팀명을 수정하거나 팀을 삭제할 수 있습니다.")
입력 항목은 팀 생성(6.4.2)과 동일하며, 기존 데이터가 미리 채워진 상태로 표시된다.

**기능**

- 저장: 팀명 변경
- 팀 삭제: 소속 선수가 없는 경우만 가능. 삭제 시 확인 다이얼로그(6.0.1) 표시

#### 6.4.4 선수 관리

팀 상세 화면에서 해당 팀의 선수를 관리한다.

**선수 목록**

- 선수 이름은 "등번호. 이름" 형식으로 표시 (예: 0. 홍길동)
- 팀장인 선수만 이름 옆에 "팀장" 뱃지를 표시한다. 팀원은 별도 표시 없음.
- 각 선수 행에 수정(✏️), 삭제(🗑️) 아이콘 버튼이 표시된다.

#### 6.4.5 선수 추가 (관리자 전용)

선수 목록 화면에서 "선수 추가" 버튼을 탭하면 선수 추가 화면으로 이동한다. 헤더에 "선수 추가"가 표시된다.

**입력 항목**

| 항목 | 타입 | 필수 |
|------|------|------|
| 이름 | 텍스트 입력 | O |
| 등번호 | 숫자 입력 | O |
| 역할 | 셀렉트 (팀장 / 팀원) | O |

**유효성 검사**

- 이름은 필수 입력
- 등번호는 0 이상의 정수
- 같은 팀 내에서 등번호 중복 불가

#### 6.4.6 선수 수정 (관리자 전용)

선수 목록 화면에서 수정(✏️) 아이콘을 탭하면 선수 수정 화면으로 이동한다. 헤더에 "선수 수정"이 표시된다.
입력 항목과 유효성 검사는 선수 추가(6.4.5)와 동일하며, 기존 데이터가 미리 채워진 상태로 표시된다.

#### 6.4.7 선수 삭제

- 선수 삭제 가능

---

### 6.5 Result (관리자 전용)

경기 결과를 등록하는 화면이다. 관리자 네비게이션에만 표시된다.

#### 6.5.1 레이아웃

- 화면 상단에 가이드 텍스트를 표시한다. (예: "경기 결과", "결과 미등록 경기를 선택하여 결과를 등록하세요.")
- 결과 미등록 일정 리스트를 표시한다. (경기 날짜가 지난 일정 기준)
- 각 일정을 탭하면 결과 등록 화면으로 이동한다.

#### 6.5.2 결과 등록 화면

헤더에 "결과 등록"이 표시된다.
상단에 가이드 텍스트를 표시한다. (예: "경기 결과 등록", "팀 스코어와 선수별 기록(골, 도움)을 입력하세요.")

**팀 스코어 입력**

- 대진 정보 표시: {팀 A} vs {팀 B}
- 팀 A 점수 입력 (숫자)
- 팀 B 점수 입력 (숫자)

**선수별 기록 입력**

- 팀 A / 팀 B 각각의 섹션으로 구분하여 표시한다.
- 각 섹션 헤더에 "{팀명} 기록" 타이틀과 "+ 기록 추가" 버튼이 표시된다.
- "+ 기록 추가"를 탭하면 선수 선택 바텀시트가 올라온다.
- 각 선수 행에 삭제(✕) 버튼이 표시된다.

**선수 선택 바텀시트**

- 반투명 오버레이 배경 위에 바텀시트 형태로 표시된다.
- 바텀시트 헤더: "선수 선택" 타이틀 + 닫기(✕) 버튼 + 안내 문구 (예: "{팀명}에서 기록할 선수를 선택하세요.")
- 해당 팀의 선수 목록이 리스트로 표시된다.
- 선수를 선택하면 해당 행이 확장되어 골/도움 스테퍼 입력이 표시된다.
  - 골: −/+ 스테퍼 (기본값 0)
  - 도움: −/+ 스테퍼 (기본값 0)
- 선택된 선수는 체크 아이콘으로 표시, 미선택 선수는 빈 원 아이콘으로 표시된다.
- 하단에 "선택 완료" 버튼이 표시된다.
- 선택 완료 시 결과 등록 화면의 기록 리스트에 선수가 추가된다.

**입력 항목 정리**

| 항목 | 타입 | 필수 |
|------|------|------|
| 팀 A 점수 | 숫자 입력 | O |
| 팀 B 점수 | 숫자 입력 | O |
| 선수 기록 (팀 A) | 선수 선택 + 골 수 + 도움 수 | X |
| 선수 기록 (팀 B) | 선수 선택 + 골 수 + 도움 수 | X |

**유효성 검사**

- 팀 점수는 0 이상의 정수
- 개인 골 합계가 팀 점수와 일치하는지 확인 (경고 표시, 등록은 가능)

**등록 완료 시**

- 순위 테이블에 자동 반영 (팀 승/패, 득점, 개인 골/도움 등)

---

## 7. 데이터 모델

### 7.1 주요 테이블

#### teams

| 칼럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 팀 고유 ID |
| name | text | 팀명 |
| created_at | timestamp | 생성일시 |

#### players

| 칼럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 선수 고유 ID |
| name | text | 선수 이름 |
| number | integer | 등번호 (같은 팀 내 고유) |
| team_id | uuid (FK → teams) | 소속 팀 |
| role | text | 역할 (leader / member) |
| created_at | timestamp | 생성일시 |

#### schedules

| 칼럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 일정 고유 ID |
| team_a_id | uuid (FK → teams) | 팀 A |
| team_b_id | uuid (FK → teams) | 팀 B |
| match_date | date | 경기 날짜 |
| match_time | time | 경기 시간 |
| location | text | 경기 장소 |
| created_at | timestamp | 생성일시 |

#### match_results

| 칼럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 결과 고유 ID |
| schedule_id | uuid (FK → schedules) | 해당 일정 |
| team_a_score | integer | 팀 A 점수 |
| team_b_score | integer | 팀 B 점수 |
| created_at | timestamp | 생성일시 |

#### player_goals

| 칼럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 기록 고유 ID |
| match_result_id | uuid (FK → match_results) | 해당 경기 결과 |
| player_id | uuid (FK → players) | 득점 선수 |
| goals | integer | 득점 수 |
| assists | integer | 도움 수 |
| created_at | timestamp | 생성일시 |

### 7.2 관계도

```
teams ─┬─< players
       ├─< schedules (team_a_id)
       └─< schedules (team_b_id)

schedules ─< match_results ─< player_goals

players ─< player_goals
```

---

## 8. 개발 로드맵

### Phase 1 — 디자인

- Figma 또는 v0로 전체 화면 디자인
- 디자인 시스템 정의 (색상, 타이포, 컴포넌트)
- 화면별 UI 디자인: Home, Rank, Schedule
- 관리자 화면 UI 디자인: Team, Result

### Phase 2 — 프로젝트 세팅 및 UI 퍼블리싱

- Vite + React + TypeScript 프로젝트 생성
- Tailwind CSS 설정
- 폴더 구조 및 라우팅 설정
- 공통 컴포넌트 구현 (Layout, BottomNav, Button 등)
- 각 화면 UI 퍼블리싱 (데이터 없이 구조만)

### Phase 3 — Mock 데이터 연동

- Mock 데이터 정의 (teams, players, schedules, results)
- TanStack Query 설정
- 각 화면에 Mock 데이터 연동
- 정렬, 필터링 등 인터랙션 구현
- 화면 간 네비게이션 완성

### Phase 4 — Supabase 연동

- Supabase 프로젝트 생성
- 데이터베이스 테이블 생성 (7. 데이터 모델 기준)
- RLS(Row Level Security) 정책 설정
- Supabase Auth 설정 (관리자 로그인)
- Mock 데이터 → Supabase 데이터로 전환
- CRUD API 연동 (팀/선수/일정/결과)

### Phase 5 — CI/CD 및 배포

- Vercel 프로젝트 연결
- GitHub 연동 및 자동 배포 설정
- 환경 변수 설정 (Supabase URL, Key)
- 프로덕션 배포 및 테스트

### Phase 6 — 개선 및 폴리싱

- 에러 핸들링 및 로딩 상태 처리
- 빈 상태(Empty State) UI
- 토스트/알림 메시지
- UI 폴리싱 및 인터랙션 개선
- 성능 최적화
- 버그 수정 및 피드백 반영

---

## 9. 비기능적 요구사항

| 항목 | 요구사항 |
|------|----------|
| 성능 | 페이지 로드 2초 이내, FCP 1초 이내 |
| 보안 | Supabase RLS 적용, 관리자 기능 인증 필수 |
| 접근성 | 모바일 앱 스타일 고정 폭 레이아웃, 주요 브라우저 지원 |
| 데이터 | Supabase 무료 플랜 기준 시작 (DB 500MB, Auth 50K MAU) |
